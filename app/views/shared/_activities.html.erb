<h3><%= t "activities" %></h3>
<hr/>

<% if activities.any? %>
  <%= render activities  %>
  <div class="text-center">
    <%= will_paginate activities %>
  </div>
<% else %>
  <%= t :no_items_found %>
<% end %>
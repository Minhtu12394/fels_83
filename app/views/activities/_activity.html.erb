<div class="list-group-item">
  <%= t :user %>
  <%= activity.behavior %>
  <%= raw activity_link_to(activity.object) unless activity.object.nil? %>
  - <%= t :at %>
  <%= time_ago_in_words activity.created_at %>
</div>
